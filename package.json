{
  "name": "walkerJS",
  "version": "0.0.3",


  "eslintConfig": {

    "env": {
      "browser": true
    },

    "globals": {
      "wjs": true
    },

    "rules": {
      "quotes": ["warn", "double"],
      "semi": "warn",
      "padded-blocks": "off"
    }
  }
}
